import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  icon: {
    backgroundColor: '#fff',
    color: '#000',

    height: 20,
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
  },
  doneText: {
    fontSize: 18,
    color: '#1054DE',
  },
});
