import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  icon: {
    backgroundColor: '#fff',
    color: '#000',
    width: 24,
    height: 20,
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
  },
  cancelText: {
    fontSize: 18,
    color: 'black',
  },
});
